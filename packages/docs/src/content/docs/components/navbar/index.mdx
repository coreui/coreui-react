---
title: "React Navbar Component"
name: "Navbar"
description: "Documentation and examples for the React navbar powerful, responsive navigation header component. Includes support for branding, links, dropdowns, and more."
otherFrameworks:
  - navbar
---

import { NavbarExample } from './examples/NavbarExample'
import NavbarExampleRaw from './examples/NavbarExample.tsx?raw'
import { NavbarBrandExample } from './examples/NavbarBrandExample'
import NavbarBrandExampleRaw from './examples/NavbarBrandExample.tsx?raw'
import { NavbarBrand2Example } from './examples/NavbarBrand2Example'
import NavbarBrand2ExampleRaw from './examples/NavbarBrand2Example.tsx?raw'
import { NavbarBrand3Example } from './examples/NavbarBrand3Example'
import NavbarBrand3ExampleRaw from './examples/NavbarBrand3Example.tsx?raw'
import { NavbarNavExample } from './examples/NavbarNavExample'
import NavbarNavExampleRaw from './examples/NavbarNavExample.tsx?raw'
import { NavbarNav2Example } from './examples/NavbarNav2Example'
import NavbarNav2ExampleRaw from './examples/NavbarNav2Example.tsx?raw'
import { NavbarNav3Example } from './examples/NavbarNav3Example'
import NavbarNav3ExampleRaw from './examples/NavbarNav3Example.tsx?raw'
import { NavbarFormsExample } from './examples/NavbarFormsExample'
import NavbarFormsExampleRaw from './examples/NavbarFormsExample.tsx?raw'
import { NavbarForms2Example } from './examples/NavbarForms2Example'
import NavbarForms2ExampleRaw from './examples/NavbarForms2Example.tsx?raw'
import { NavbarForms3Example } from './examples/NavbarForms3Example'
import NavbarForms3ExampleRaw from './examples/NavbarForms3Example.tsx?raw'
import { NavbarForms4Example } from './examples/NavbarForms4Example'
import NavbarForms4ExampleRaw from './examples/NavbarForms4Example.tsx?raw'
import { NavbarText } from './examples/NavbarText'
import NavbarTextRaw from './examples/NavbarText.tsx?raw'
import { NavbarColorSchemesExample } from './examples/NavbarColorSchemesExample'
import NavbarColorSchemesExampleRaw from './examples/NavbarColorSchemesExample.tsx?raw'
import { NavbarContainersExample } from './examples/NavbarContainersExample'
import NavbarContainersExampleRaw from './examples/NavbarContainersExample.tsx?raw'
import { NavbarContainers2Example } from './examples/NavbarContainers2Example'
import NavbarContainers2ExampleRaw from './examples/NavbarContainers2Example.tsx?raw'
import { NavbarPlacementExample } from './examples/NavbarPlacementExample'
import NavbarPlacementExampleRaw from './examples/NavbarPlacementExample.tsx?raw'
import { NavbarPlacementFixedTopExample } from './examples/NavbarPlacementFixedTopExample'
import NavbarPlacementFixedTopExampleRaw from './examples/NavbarPlacementFixedTopExample.tsx?raw'
import { NavbarPlacementFixedBottomExample } from './examples/NavbarPlacementFixedBottomExample'
import NavbarPlacementFixedBottomExampleRaw from './examples/NavbarPlacementFixedBottomExample.tsx?raw'
import { NavbarPlacementStickyTopExample } from './examples/NavbarPlacementStickyTopExample'
import NavbarPlacementStickyTopExampleRaw from './examples/NavbarPlacementStickyTopExample.tsx?raw'
import { NavbarResponsiveTogglerExample } from './examples/NavbarResponsiveTogglerExample'
import NavbarResponsiveTogglerExampleRaw from './examples/NavbarResponsiveTogglerExample.tsx?raw'
import { NavbarResponsiveToggler2Example } from './examples/NavbarResponsiveToggler2Example'
import NavbarResponsiveToggler2ExampleRaw from './examples/NavbarResponsiveToggler2Example.tsx?raw'
import { NavbarResponsiveToggler3Example } from './examples/NavbarResponsiveToggler3Example'
import NavbarResponsiveToggler3ExampleRaw from './examples/NavbarResponsiveToggler3Example.tsx?raw'
import { NavbarResponsiveExternalContentExample } from './examples/NavbarResponsiveExternalContentExample'
import NavbarResponsiveExternalContentExampleRaw from './examples/NavbarResponsiveExternalContentExample.tsx?raw'
import { NavbarResponsiveOffcanvasExample } from './examples/NavbarResponsiveOffcanvasExample'
import NavbarResponsiveOffcanvasExampleRaw from './examples/NavbarResponsiveOffcanvasExample.tsx?raw'
import { NavbarResponsiveOffcanvas2Example } from './examples/NavbarResponsiveOffcanvas2Example'
import NavbarResponsiveOffcanvas2ExampleRaw from './examples/NavbarResponsiveOffcanvas2Example.tsx?raw'

## Supported content

`<CNavbar>` come with built-in support for a handful of sub-components. Choose from the following as needed:

- `<CNavbarBrand>` for your company, product, or project name.
- `<CNavbarNav>` for a full-height and lightweight navigation (including support for dropdowns).
- `<CNavbarToggler>` for use with our collapse plugin and other [navigation toggling](#responsive-behaviors) behaviors.
- Flex and spacing utilities for any form controls and actions.
- `<CNavbarText>` for adding vertically centered strings of text.
- `<CCollapse>` for grouping and hiding navbar contents by a parent breakpoint.

Here's an example of all the sub-components included in a responsive light-themed navbar that automatically collapses at the `lg` (large) breakpoint.

## Basic usage

<Example code={NavbarExampleRaw} name="NavbarExample" componentName="React Navbar">
  <NavbarExample client:only="react" />
</Example>

### Brand

The `<CNavbarBrand>` can be applied to most elements, but an anchor works best, as some elements might require utility classes or custom styles.

<Example code={NavbarBrandExampleRaw} name="NavbarBrandExample" componentName="React Navbar">
  <NavbarBrandExample client:only="react" />
</Example>

Adding images to the `<CNavbarBrand>` will likely always require custom styles or utilities to properly size. Here are some examples to demonstrate.

<Example code={NavbarBrand2ExampleRaw} name="NavbarBrand2Example" componentName="React Navbar">
  <NavbarBrand2Example client:only="react" />
</Example>

<Example code={NavbarBrand3ExampleRaw} name="NavbarBrand3Example" componentName="React Navbar">
  <NavbarBrand3Example client:only="react" />
</Example>

### Nav

`<CNavbar>` navigation is based on `<CNavbarNav>`. **Navigation in navbars will also grow to occupy as much horizontal space as possible** to keep your navbar contents securely aligned.

<Example code={NavbarNavExampleRaw} name="NavbarNavExample" componentName="React Navbar">
  <NavbarNavExample client:only="react" />
</Example>

And because we use classes for our navs, you can avoid the list-based approach entirely if you like.

<Example code={NavbarNav2ExampleRaw} name="NavbarNav2Example" componentName="React Navbar">
  <NavbarNav2Example client:only="react" />
</Example>

You can also use dropdowns in your navbar. Please note that `<CDropdown>` component requires `variant="nav-item"`.

<Example code={NavbarNav3ExampleRaw} name="NavbarNav3Example" componentName="React Navbar">
  <NavbarNav3Example client:only="react" />
</Example>

### Forms

Place various form controls and components within a navbar:

<Example code={NavbarFormsExampleRaw} name="NavbarFormsExample" componentName="React Navbar">
  <NavbarFormsExample client:only="react" />
</Example>

Immediate child elements of `<CNavbar>` use flex layout and will default to `justify-content: space-between`. Use additional [flex utilities](https://coreui.io/bootstrap/docs/utilities/flex/) as needed to adjust this behavior.

<Example code={NavbarForms2ExampleRaw} name="NavbarForms2Example" componentName="React Navbar">
  <NavbarForms2Example client:only="react" />
</Example>

Input groups work, too. If your navbar is an entire form, or mostly a form, you can use the `<CForm>` element as the container and save some HTML.

<Example code={NavbarForms3ExampleRaw} name="NavbarForms3Example" componentName="React Navbar">
  <NavbarForms3Example client:only="react" />
</Example>

Various buttons are supported as part of these navbar forms, too. This is also a great reminder that vertical alignment utilities can be used to align different sized elements.

<Example code={NavbarForms4ExampleRaw} name="NavbarForms4Example" componentName="React Navbar">
  <NavbarForms4Example client:only="react" />
</Example>

### Text

Navbars may contain bits of text with the help of `<CNavbarText>`. This class adjusts vertical alignment and horizontal spacing for strings of text.

<Example code={NavbarTextRaw} name="NavbarText" componentName="React Navbar">
  <NavbarText client:only="react" />
</Example>

## Color schemes

Theming the navbar has never been easier thanks to the combination of theming classes and `background-color` utilities. Set `colorScheme="light"` for use with light background colors, or `colorScheme="dark"` for dark background colors. Then, customize with `.bg-*` utilities.

<Example code={NavbarColorSchemesExampleRaw} name="NavbarColorSchemesExample" componentName="React Navbar">
  <NavbarColorSchemesExample client:only="react" />
</Example>

## Containers

Although it's not required, you can wrap a `<CNavbar>` in a `<CContainer>` to center it on a page–though note that an inner container is still required. Or you can add a container inside the `<CNavbar>` to only center the contents of a [fixed or static top navbar](#placement).

<Example code={NavbarContainersExampleRaw} name="NavbarContainersExample" componentName="React Navbar">
  <NavbarContainersExample client:only="react" />
</Example>

Use any of the responsive containers to change how wide the content in your navbar is presented.

<Example code={NavbarContainers2ExampleRaw} name="NavbarContainers2Example" componentName="React Navbar">
  <NavbarContainers2Example client:only="react" />
</Example>

## Placement

Use our `placement` properly to place navbars in non-static positions. Choose from fixed to the top, fixed to the bottom, or stickied to the top (scrolls with the page until it reaches the top, then stays there). Fixed navbars use `position: fixed`, meaning they're pulled from the normal flow of the DOM and may require custom CSS (e.g., `padding-top` on the `<body>`) to prevent overlap with other elements.

Also note that **`.sticky-top` uses `position: sticky`, which [isn't fully supported in every browser](https://caniuse.com/css-sticky)**.

<Example code={NavbarPlacementExampleRaw} name="NavbarPlacementExample" componentName="React Navbar">
  <NavbarPlacementExample client:only="react" />
</Example>

<Example code={NavbarPlacementFixedTopExampleRaw} name="NavbarPlacementFixedTopExample" componentName="React Navbar" class="p-4 overflow-hidden">
  <NavbarPlacementFixedTopExample client:only="react" />
</Example>

<Example code={NavbarPlacementFixedBottomExampleRaw} name="NavbarPlacementFixedBottomExample" componentName="React Navbar" class="p-4">
  <NavbarPlacementFixedBottomExample client:only="react" />
</Example>

<Example code={NavbarPlacementStickyTopExampleRaw} name="NavbarPlacementStickyTopExample" componentName="React Navbar" class="p-4 overflow-hidden">
  <NavbarPlacementStickyTopExample client:only="react" />
</Example>

## Responsive behaviors

Navbars can use `<CNavbarToggler>`, `<CCollapse>`, and `expand="{sm|md|lg|xl|xxl}"` property to determine when their content collapses behind a button. In combination with other utilities, you can easily choose when to show or hide particular elements.

For navbars that never collapse, add the `expand` boolean property on the `<CNavbar>`. For navbars that always collapse, don't add any property.

### Toggler

Navbar togglers are left-aligned by default, but should they follow a sibling element like a `<CNavbarBrand>`, they'll automatically be aligned to the far right. Reversing your markup will reverse the placement of the toggler. Below are examples of different toggle styles.

With no `<CNavbarBrand>` shown at the smallest breakpoint:

<Example code={NavbarResponsiveTogglerExampleRaw} name="NavbarResponsiveTogglerExample" componentName="React Navbar">
  <NavbarResponsiveTogglerExample client:only="react" />
</Example>

With a brand name shown on the left and toggler on the right:

<Example code={NavbarResponsiveToggler2ExampleRaw} name="NavbarResponsiveToggler2Example" componentName="React Navbar">
  <NavbarResponsiveToggler2Example client:only="react" />
</Example>

With a toggler on the left and brand name on the right:

<Example code={NavbarResponsiveToggler3ExampleRaw} name="NavbarResponsiveToggler3Example" componentName="React Navbar">
  <NavbarResponsiveToggler3Example client:only="react" />
</Example>

### External content

Sometimes you want to use the collapse plugin to trigger a container element for content that structurally sits outside of the `<CNavbar>`.

<Example code={NavbarResponsiveExternalContentExampleRaw} name="NavbarResponsiveExternalContentExample" componentName="React Navbar">
  <NavbarResponsiveExternalContentExample client:only="react" />
</Example>

### Offcanvas

Transform your expanding and collapsing navbar into an offcanvas drawer with the offcanvas plugin. We extend both the offcanvas default styles and use our `expand="*"` prop to create a dynamic and flexible navigation sidebar.

In the example below, to create an offcanvas navbar that is always collapsed across all breakpoints, omit the `expand="*"` prop entirely.

<Example code={NavbarResponsiveOffcanvasExampleRaw} name="NavbarResponsiveOffcanvasExample" componentName="React Navbar">
  <NavbarResponsiveOffcanvasExample client:only="react" />
</Example>

To create an offcanvas navbar that expands into a normal navbar at a specific breakpoint like `xxl`, use `expand="xxl"` property.

<Example code={NavbarResponsiveOffcanvas2ExampleRaw} name="NavbarResponsiveOffcanvas2Example" componentName="React Navbar">
  <NavbarResponsiveOffcanvas2Example client:only="react" />
</Example>

## API

Check out the documentation below for a comprehensive guide to all the props you can use with the components mentioned here.

- [&lt;CNavbar /&gt;](./api/#cnavbar)
- [&lt;CNavbarBrand /&gt;](./api/#cnavbarbrand)
- [&lt;CNavbarNav /&gt;](./api/#cnavbarnav)
- [&lt;CNavbarText /&gt;](./api/#cnavbartext)
- [&lt;CNavbarToggler /&gt;](./api/#cnavbartoggler)