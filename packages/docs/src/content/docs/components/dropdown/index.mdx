---
title: "React Dropdown Component"
name: "Dropdown"
description: "React dropdown component allows you to toggle contextual overlays for displaying lists, links, and more html elements."
otherFrameworks:
  - dropdown
---

import { DropdownSingleButtonExample } from './examples/DropdownSingleButtonExample'
import DropdownSingleButtonExampleRaw from './examples/DropdownSingleButtonExample.tsx?raw'
import { DropdownSingleButton2Example } from './examples/DropdownSingleButton2Example'
import DropdownSingleButton2ExampleRaw from './examples/DropdownSingleButton2Example.tsx?raw'
import { DropdownSingleButton3Example } from './examples/DropdownSingleButton3Example'
import DropdownSingleButton3ExampleRaw from './examples/DropdownSingleButton3Example.tsx?raw'
import { DropdownSplitButtonExample } from './examples/DropdownSplitButtonExample'
import DropdownSplitButtonExampleRaw from './examples/DropdownSplitButtonExample.tsx?raw'
import { DropdownSizingLargeExample } from './examples/DropdownSizingLargeExample'
import DropdownSizingLargeExampleRaw from './examples/DropdownSizingLargeExample.tsx?raw'
import { DropdownSizingSmallExample } from './examples/DropdownSizingSmallExample'
import DropdownSizingSmallExampleRaw from './examples/DropdownSizingSmallExample.tsx?raw'
import { DropdownDarkExample } from './examples/DropdownDarkExample'
import DropdownDarkExampleRaw from './examples/DropdownDarkExample.tsx?raw'
import { DropdownDark2Example } from './examples/DropdownDark2Example'
import DropdownDark2ExampleRaw from './examples/DropdownDark2Example.tsx?raw'
import { DropdownCenteredExample } from './examples/DropdownCenteredExample'
import DropdownCenteredExampleRaw from './examples/DropdownCenteredExample.tsx?raw'
import { DropdownDropupExample } from './examples/DropdownDropupExample'
import DropdownDropupExampleRaw from './examples/DropdownDropupExample.tsx?raw'
import { DropdownDropupCenteredExample } from './examples/DropdownDropupCenteredExample'
import DropdownDropupCenteredExampleRaw from './examples/DropdownDropupCenteredExample.tsx?raw'
import { DropdownDropendExample } from './examples/DropdownDropendExample'
import DropdownDropendExampleRaw from './examples/DropdownDropendExample.tsx?raw'
import { DropdownDropstartExample } from './examples/DropdownDropstartExample'
import DropdownDropstartExampleRaw from './examples/DropdownDropstartExample.tsx?raw'
import { DropdownMenuItemsExample } from './examples/DropdownMenuItemsExample'
import DropdownMenuItemsExampleRaw from './examples/DropdownMenuItemsExample.tsx?raw'
import { DropdownMenuItems2Example } from './examples/DropdownMenuItems2Example'
import DropdownMenuItems2ExampleRaw from './examples/DropdownMenuItems2Example.tsx?raw'
import { DropdownMenuItemsActiveExample } from './examples/DropdownMenuItemsActiveExample'
import DropdownMenuItemsActiveExampleRaw from './examples/DropdownMenuItemsActiveExample.tsx?raw'
import { DropdownMenuItemsDisabledExample } from './examples/DropdownMenuItemsDisabledExample'
import DropdownMenuItemsDisabledExampleRaw from './examples/DropdownMenuItemsDisabledExample.tsx?raw'
import { DropdownMenuAlignmentExample } from './examples/DropdownMenuAlignmentExample'
import DropdownMenuAlignmentExampleRaw from './examples/DropdownMenuAlignmentExample.tsx?raw'
import { DropdownResponsiveAlignmentExample } from './examples/DropdownResponsiveAlignmentExample'
import DropdownResponsiveAlignmentExampleRaw from './examples/DropdownResponsiveAlignmentExample.tsx?raw'
import { DropdownResponsiveAlignment2Example } from './examples/DropdownResponsiveAlignment2Example'
import DropdownResponsiveAlignment2ExampleRaw from './examples/DropdownResponsiveAlignment2Example.tsx?raw'
import { DropdownMenuContentHeadersExample } from './examples/DropdownMenuContentHeadersExample'
import DropdownMenuContentHeadersExampleRaw from './examples/DropdownMenuContentHeadersExample.tsx?raw'
import { DropdownMenuContentDividersExample } from './examples/DropdownMenuContentDividersExample'
import DropdownMenuContentDividersExampleRaw from './examples/DropdownMenuContentDividersExample.tsx?raw'
import { DropdownMenuContentTextExample } from './examples/DropdownMenuContentTextExample'
import DropdownMenuContentTextExampleRaw from './examples/DropdownMenuContentTextExample.tsx?raw'
import { DropdownMenuContentFormsExample } from './examples/DropdownMenuContentFormsExample'
import DropdownMenuContentFormsExampleRaw from './examples/DropdownMenuContentFormsExample.tsx?raw'
import { DropdownOptionsExample } from './examples/DropdownOptionsExample'
import DropdownOptionsExampleRaw from './examples/DropdownOptionsExample.tsx?raw'
import { DropdownOptionsAutoCloseBehaviorExample } from './examples/DropdownOptionsAutoCloseBehaviorExample'
import DropdownOptionsAutoCloseBehaviorExampleRaw from './examples/DropdownOptionsAutoCloseBehaviorExample.tsx?raw'

## Overview

Dropdowns are toggleable, contextual overlays for displaying lists of links and more.

Dropdowns are built on a third party library, [Popper.js](https://popper.js.org/), which provides dynamic positioning and viewport detection. Popper.js isn't used to position dropdowns in navbars though as dynamic positioning isn't required.

## Examples

Bind the dropdown's toggle and the dropdown menu inside `<CDropdown>`, or different element that declares `position: relative;`. Dropdowns can be triggered from `<a>` or `<button>` elements to better fit your possible requirements.

### Single button

Here's how you can put them to work with either `<button>` elements:

<Example code={DropdownSingleButtonExampleRaw} name="DropdownSingleButtonExample" componentName="React Dropdown">
  <DropdownSingleButtonExample client:only="react" />
</Example>

And with `<a>` elements:

<Example code={DropdownSingleButton2ExampleRaw} name="DropdownSingleButton2Example" componentName="React Dropdown">
  <DropdownSingleButton2Example client:only="react" />
</Example>

The best part is you can do this with any button variant, too:

<Example code={DropdownSingleButton3ExampleRaw} name="DropdownSingleButton3Example" componentName="React Dropdown">
  <DropdownSingleButton3Example client:only="react" />
</Example>

### Split button

Similarly, create split button dropdowns with virtually the same markup as single button dropdowns, but with the addition of boolean prop `split` for proper spacing around the dropdown caret.

We use this extra class to reduce the horizontal `padding` on either side of the caret by 25% and remove the `margin-left` that's attached for normal button dropdowns. Those additional changes hold the caret centered in the split button and implement a more properly sized hit area next to the main button.

<Example code={DropdownSplitButtonExampleRaw} name="DropdownSplitButtonExample" componentName="React Dropdown">
  <DropdownSplitButtonExample client:only="react" />
</Example>

## Sizing

Button dropdowns work with buttons of all sizes, including default and split dropdown buttons.

<Example code={DropdownSizingLargeExampleRaw} name="DropdownSizingLargeExample" componentName="React Dropdown">
  <DropdownSizingLargeExample client:only="react" />
</Example>

<Example code={DropdownSizingSmallExampleRaw} name="DropdownSizingSmallExample" componentName="React Dropdown">
  <DropdownSizingSmallExample client:only="react" />
</Example>

## Dark dropdowns

Opt into darker dropdowns to match a dark navbar or custom style by set `dark` property. No changes are required to the dropdown items.

<Example code={DropdownDarkExampleRaw} name="DropdownDarkExample" componentName="React Dropdown">
  <DropdownDarkExample client:only="react" />
</Example>

And putting it to use in a navbar:

<Example code={DropdownDark2ExampleRaw} name="DropdownDark2Example" componentName="React Dropdown">
  <DropdownDark2Example client:only="react" />
</Example>


## Directions

<Callout color="info" title="RTL">
  Directions are mirrored when using CoreUI in RTL, meaning `.dropstart` will appear on the right side.
</Callout>

### Centered

Make the dropdown menu centered below the toggle by adding `direction="center"` to the `<CDropdown>` component.

<Example code={DropdownCenteredExampleRaw} name="DropdownCenteredExample" componentName="React Dropdown">
  <DropdownCenteredExample client:only="react" />
</Example>

### Dropup

Trigger dropdown menus above elements by adding `direction="dropup"` to the `<CDropdown>` component.

<Example code={DropdownDropupExampleRaw} name="DropdownDropupExample" componentName="React Dropdown">
  <DropdownDropupExample client:only="react" />
</Example>

### Dropup centered

Make the dropup menu centered above the toggle by adding `direction="dropup-center"` to the `<CDropdown>` component.

<Example code={DropdownDropupCenteredExampleRaw} name="DropdownDropupCenteredExample" componentName="React Dropdown">
  <DropdownDropupCenteredExample client:only="react" />
</Example>

### Dropend

Trigger dropdown menus at the right of the elements by adding `direction="dropend"` to the `<CDropdown>` component.

<Example code={DropdownDropendExampleRaw} name="DropdownDropendExample" componentName="React Dropdown">
  <DropdownDropendExample client:only="react" />
</Example>

### Dropstart

Trigger dropdown menus at the left of the elements by adding `direction="dropstart"` to the `<CDropdown>` component.

<Example code={DropdownDropstartExampleRaw} name="DropdownDropstartExample" componentName="React Dropdown">
  <DropdownDropstartExample client:only="react" />
</Example>

## Menu items

Historically dropdown menu contents _had_ to be links, but that's no longer the case with v4. Now you can optionally use `<button>` elements in your dropdowns instead of just `<a>`s.

<Example code={DropdownMenuItemsExampleRaw} name="DropdownMenuItemsExample" componentName="React Dropdown">
  <DropdownMenuItemsExample client:only="react" />
</Example>

You can also create non-interactive dropdown items with `<CDropdownItemPlain>`.

<Example code={DropdownMenuItems2ExampleRaw} name="DropdownMenuItems2Example" componentName="React Dropdown">
  <DropdownMenuItems2Example client:only="react" />
</Example>

### Active

Set boolean property `active` to **style item as active**.

In the following example we use `div` instead of `<CDropdownMenu>` to show `<CDropdownMenu>` content.

<Example code={DropdownMenuItemsActiveExampleRaw} name="DropdownMenuItemsActiveExample" componentName="React Dropdown">
  <DropdownMenuItemsActiveExample client:only="react" />
</Example>

### Disabled

Add `disabled` boolean property to items in the dropdown to **style them as disabled**.

In the following example we use `div` instead of `<CDropdownMenu>` to show `<CDropdownMenu>` content.

<Example code={DropdownMenuItemsDisabledExampleRaw} name="DropdownMenuItemsDisabledExample" componentName="React Dropdown">
  <DropdownMenuItemsDisabledExample client:only="react" />
</Example>

## Menu alignment

By default, a dropdown menu is automatically positioned 100% from the top and along the left side of its parent. Add `aligment="end"` to right align the dropdown menu.

<Callout color="info">
  <strong>Heads up!</strong> Dropdowns are positioned thanks to Popper.
</Callout>

<Example code={DropdownMenuAlignmentExampleRaw} name="DropdownMenuAlignmentExample" componentName="React Dropdown">
  <DropdownMenuAlignmentExample client:only="react" />
</Example>

### Responsive alignment

If you use responsive alignment, dynamic positioning is disabled.

To align **right** the dropdown menu with the given breakpoint or larger, add `aligment="xs|sm|md|lg|xl|xxl: end"`.

<Example code={DropdownResponsiveAlignmentExampleRaw} name="DropdownResponsiveAlignmentExample" componentName="React Dropdown">
  <DropdownResponsiveAlignmentExample client:only="react" />
</Example>

To align **left** the dropdown menu with the given breakpoint or larger, add `aligment="xs|sm|md|lg|xl|xxl: start"`.

<Example code={DropdownResponsiveAlignment2ExampleRaw} name="DropdownResponsiveAlignment2Example" componentName="React Dropdown">
  <DropdownResponsiveAlignment2Example client:only="react" />
</Example>


## Menu content

### Headers

Add a header to label sections of actions in any dropdown menu.

In the following example we use `div` instead of `<CDropdownMenu>` to show `<CDropdownMenu>` content.

<Example code={DropdownMenuContentHeadersExampleRaw} name="DropdownMenuContentHeadersExample" componentName="React Dropdown">
  <DropdownMenuContentHeadersExample client:only="react" />
</Example>

### Dividers

Separate groups of related menu items with a divider.

In the following example we use `div` instead of `<CDropdownMenu>` to show `<CDropdownMenu>` content.

<Example code={DropdownMenuContentDividersExampleRaw} name="DropdownMenuContentDividersExample" componentName="React Dropdown">
  <DropdownMenuContentDividersExample client:only="react" />
</Example>

### Text

Place any freeform text within a dropdown menu with text. Note that you'll likely need additional sizing styles to constrain the menu width.

<Example code={DropdownMenuContentTextExampleRaw} name="DropdownMenuContentTextExample" componentName="React Dropdown">
  <DropdownMenuContentTextExample client:only="react" />
</Example>

### Forms

Put a form within a dropdown menu, or make it into a dropdown menu.

<Example code={DropdownMenuContentFormsExampleRaw} name="DropdownMenuContentFormsExample" componentName="React Dropdown">
  <DropdownMenuContentFormsExample client:only="react" />
</Example>

## Dropdown options

Use `offset` to displace the dropdown from its default position. The value is a string with two numbers separated by a comma, e.g. `offset={[10, 20]}`. Use `portal` property to render dropdowns in `body` instead of the parent element. This helps to avoid any overflow or z-index issues.

<Example code={DropdownOptionsExampleRaw} name="DropdownOptionsExample" componentName="React Dropdown">
  <DropdownOptionsExample client:only="react" />
</Example>

### Auto close behavior

By default, dropdowns are closed when clicking outside of the dropdown menu or the toggle button. You can change this behavior with the `autoClose` property. Set `autoClose` to: 

- `true` - Close on clicks inside or outside of the React.js dropdown menu.
- `false` - Disable auto-close; close manually by setting the `visible={false}` (also not closed by `Escape`).
- `'inside'` - Close only when clicking inside the React.js dropdown menu.
- `'outside'` - Close only when clicking outside the React.js dropdown menu.

<Example code={DropdownOptionsAutoCloseBehaviorExampleRaw} name="DropdownOptionsAutoCloseBehaviorExample" componentName="React Dropdown">
  <DropdownOptionsAutoCloseBehaviorExample client:only="react" />
</Example>

## API

Check out the documentation below for a comprehensive guide to all the props you can use with the components mentioned here.

- [&lt;CDropdown /&gt;](./api/#cdropdown)
- [&lt;CDropdownDivider /&gt;](./api/#cdropdowndivider)
- [&lt;CDropdownHeader /&gt;](./api/#cdropdownheader)
- [&lt;CDropdownItem /&gt;](./api/#cdropdownitem)
- [&lt;CDropdownItemPlain /&gt;](./api/#cdropdownitemplain)
- [&lt;CDropdownMenu /&gt;](./api/#cdropdownmenu)
- [&lt;CDropdownToggle /&gt;](./api/#cdropdowntoggle)
