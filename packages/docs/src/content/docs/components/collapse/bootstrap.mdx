---
title: "React Bootstrap Collapse Component"
name: "Collapse with Bootstrap Styling"
description: "Learn how to use the React Bootstrap Collapse component from CoreUI to toggle visibility of content panels using animations and Bootstrap-compatible transitions."
bootstrap: true
---

import { CollapseExample } from './examples/CollapseExample'
import CollapseExampleRaw from './examples/CollapseExample.tsx?raw'
import { CollapseHorizontalExample } from './examples/CollapseHorizontalExample'
import CollapseHorizontalExampleRaw from './examples/CollapseHorizontalExample.tsx?raw'
import { CollapseMultipleTargetsExample } from './examples/CollapseMultipleTargetsExample'
import CollapseMultipleTargetsExampleRaw from './examples/CollapseMultipleTargetsExample.tsx?raw'

## How the React Bootstrap Collapse component works

The React Bootstrap Collapse component allows you to toggle the visibility of content using animated height or width transitions. It’s perfect for creating expandable sections, FAQs, and collapsible panels with clean and accessible markup.

Triggers such as buttons or anchor elements are connected to collapsible targets. The collapse animation modifies the `height` or `width` property, so padding should not be applied directly to the `.collapse` element.

## React Bootstrap Collapse example

You can trigger collapsible content using a button or a link. Below is a simple example using CoreUI components styled with Bootstrap.

<Example code={CollapseExampleRaw} name="CollapseExample" componentName="React Bootstrap Collapse" class="theme-bootstrap">
  <CollapseExample client:only="react" />
</Example>

## Horizontal React Bootstrap Collapse

Use the `horizontal` prop on the React Bootstrap Collapse component to transition the `width` instead of `height`. Don’t forget to apply a specific width to the immediate child of the collapse element.

<Example code={CollapseHorizontalExampleRaw} name="CollapseHorizontalExample" componentName="React Bootstrap Collapse" class="theme-bootstrap">
  <CollapseHorizontalExample client:only="react" />
</Example>

## Multiple collapse targets

React Bootstrap Collapse supports multiple targets toggled by a single trigger. Use a `<CButton>` to show or hide more than one collapsible element at the same time.

<Example code={CollapseMultipleTargetsExampleRaw} name="CollapseMultipleTargetsExample" componentName="React Bootstrap Collapse" class="theme-bootstrap">
  <CollapseMultipleTargetsExample client:only="react" />
</Example>

## API reference

Check the API documentation below to explore all available props for the React Bootstrap Collapse component:

- [&lt;CCollapse /&gt;](../api/#ccollapse)
