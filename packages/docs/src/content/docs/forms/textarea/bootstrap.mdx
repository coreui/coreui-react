---
title: "React Bootstrap Textarea Component"
name: "Form Textarea with Bootstrap Styling"
description: "Learn how to style and enhance Bootstrap-style textarea fields in React using CoreUI components with custom sizing, disabled states, and validation feedback."
bootstrap: true
otherFrameworks:
  - textarea
---

import { FormTextareaExample } from './examples/FormTextareaExample'
import FormTextareaExampleRaw from './examples/FormTextareaExample.tsx?raw'
import { FormTextareaDisabledExample } from './examples/FormTextareaDisabledExample'
import FormTextareaDisabledExampleRaw from './examples/FormTextareaDisabledExample.tsx?raw'
import { FormTextareaReadonlyExample } from './examples/FormTextareaReadonlyExample'
import FormTextareaReadonlyExampleRaw from './examples/FormTextareaReadonlyExample.tsx?raw'

## How to use React Bootstrap Textarea component

Use `<CFormTextarea>` to create multi-line text inputs in your forms, styled to match Bootstrap’s design system.

### Basic example

Use the component with a label and help text for accessibility and usability.

<Example code={FormTextareaExampleRaw} lang="tsx" name="FormTextareaExample" componentName="React Bootstrap Textarea" class="theme-bootstrap">
  <FormTextareaExample client:only="react" />
</Example>

You can also build the same structure manually using individual form components:

```jsx
<CFormLabel htmlFor="exampleFormControlTextarea1">Example textarea</CFormLabel>
<CFormTextarea id="exampleFormControlTextarea1" rows={3}></CFormTextarea>
<CFormText as="span" id="passwordHelpInline">Must be 8-20 words long.</CFormText>
```

### Disabled state

Add the `disabled` prop to make the textarea inactive and visually grayed out.

<Example code={FormTextareaDisabledExampleRaw} lang="tsx" name="FormTextareaDisabledExample" componentName="React Bootstrap Textarea" class="theme-bootstrap">
  <FormTextareaDisabledExample client:only="react" />
</Example>

### Read-only

Use the `readOnly` prop to prevent edits while still allowing text selection and copying.

<Example code={FormTextareaReadonlyExampleRaw} lang="tsx" name="FormTextareaReadonlyExample" componentName="React Bootstrap Textarea" class="theme-bootstrap">
  <FormTextareaReadonlyExample client:only="react" />
</Example>

## API reference

Refer to the API documentation below for a complete list of available props and subcomponents used to build Bootstrap-style textareas in React.

- [&lt;CFormTextarea /&gt;](../api/#cformtextarea)
- [&lt;CFormFeedback /&gt;](../api/#cformfeedback)
- [&lt;CFormLabel /&gt;](../api/#cformlabel)
- [&lt;CFormText /&gt;](../api/#cformtext)
