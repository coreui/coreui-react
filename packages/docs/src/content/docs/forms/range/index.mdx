---
title: "React Range Component"
name: "Range"
description: "React range component. Use our custom range inputs for consistent cross-browser styling and built-in customization."
otherFrameworks:
  - range
---

import { FormRangeExample } from './examples/FormRangeExample'
import FormRangeExampleRaw from './examples/FormRangeExample.tsx?raw'
import { FormRangeDisabledExample } from './examples/FormRangeDisabledExample'
import FormRangeDisabledExampleRaw from './examples/FormRangeDisabledExample.tsx?raw'
import { FormRangeMinAndMaxExample } from './examples/FormRangeMinAndMaxExample'
import FormRangeMinAndMaxExampleRaw from './examples/FormRangeMinAndMaxExample.tsx?raw'
import { FormRangeStepsExample } from './examples/FormRangeStepsExample'
import FormRangeStepsExampleRaw from './examples/FormRangeStepsExample.tsx?raw'

## Overview

Create custom `<input type="range">` controls with `<CFormRange>`. The track (the background) and thumb (the value) are both styled to appear the same across browsers. As only Edge Legacy and Firefox supports "filling" their track from the left or right of the thumb as a means to visually indicate progress, we do not currently support it.

<Example code={FormRangeExampleRaw} lang="tsx" name="FormRangeExample" componentName="React Form Range">
  <FormRangeExample client:only="react" />
</Example>

## Disabled

Add the `disabled` boolean attribute on an input to give it a grayed out appearance and remove pointer events.

<Example code={FormRangeDisabledExampleRaw} lang="tsx" name="FormRangeDisabledExample" componentName="React Form Range">
  <FormRangeDisabledExample client:only="react" />
</Example>

## Min and max

Range inputs have implicit values for `min` and `max`—`0` and `100`, respectively. You may specify new values for those using the `min` and `max` attributes.

<Example code={FormRangeMinAndMaxExampleRaw} lang="tsx" name="FormRangeMinAndMaxExample" componentName="React Form Range">
  <FormRangeMinAndMaxExample client:only="react" />
</Example>

## Steps

By default, range inputs "snap" to integer values. To change this, you can specify a `step` value. In the example below, we double the number of steps by using `step={0.5}`.

<Example code={FormRangeStepsExampleRaw} lang="tsx" name="FormRangeStepsExample" componentName="React Form Range">
  <FormRangeStepsExample client:only="react" />
</Example>

## API

Check out the documentation below for a comprehensive guide to all the props you can use with the components mentioned here.

- [&lt;CFormRange /&gt;](./api/#cformrange)
