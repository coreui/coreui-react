| Prop name | Description | Type |
| --- | --- | --- |
| active | Toggle the active state for the component. [docs] | boolean |
| caret | Enables pseudo element caret on toggler. [docs] | boolean |
| className | A string of all className you want applied to the base component. [docs] | string |
| color | Sets the color context of the component to one of CoreUI’s themed colors. [docs] | 'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'dark' | 'light' | string |
| component | Component used for the root node. Either a string to use a HTML element or a component. [docs] | string | ComponentClass<any, any> | FunctionComponent<any> |
| disabled | Toggle the disabled state for the component. [docs] | boolean |
| href | The href attribute specifies the URL of the page the link goes to. [docs] | string |
| role | The role attribute describes the role of an element in programs that can make use of it, such as screen readers or magnifiers. [docs] | string |
| shape | Select the shape of the component. [docs] | 'rounded' | 'rounded-top' | 'rounded-end' | 'rounded-bottom' | 'rounded-start' | 'rounded-circle' | 'rounded-pill' | 'rounded-0' | 'rounded-1' | 'rounded-2' | 'rounded-3' | string |
| size | Size the component small or large. [docs] | 'sm' | 'lg' |
| split | Similarly, create split button dropdowns with virtually the same markup as single button dropdowns, but with the addition of `.dropdown-toggle-split` className for proper spacing around the dropdown caret. [docs] | boolean |
| trigger | Sets which event handlers you’d like provided to your toggle prop. You can specify one trigger or an array of them. [docs] | Triggers | Triggers[] |
| type | Specifies the type of button. Always specify the type attribute for the `<button>` element.
Different browsers may use different default types for the `<button>` element. [docs] | 'button' | 'submit' | 'reset' |
| variant | Set the button variant to an outlined button or a ghost button. [docs] | "outline" | "ghost" |
